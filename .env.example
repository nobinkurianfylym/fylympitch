NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
NEXT_PUBLIC_SITE_URL=https://fylympitch.netlify.app

# Server-only — used by services/fylympitchEngine.ts (AI Executive Producer brief).
# No NEXT_PUBLIC_ prefix: must never be exposed to the browser.
# If unset, the engine falls back to a deterministic heuristic brief.
OPENAI_API_KEY=sk-...
